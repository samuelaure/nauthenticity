generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}


model Post {
  id              String   @id @default(uuid())
  instagramId     String?  @unique
  instagramUrl    String   @unique
  username        String?
  igProfile       IgProfile? @relation(fields: [username], references: [username])
  caption         String?
  originalCaption String?
  postedAt        DateTime
  likes           Int      @default(0)
  comments        Int      @default(0)
  views           Int?
  engagementScore Float?

  collaborators   Json?
  intelligence    Json?

  runId           String?
  run             ScrapingRun? @relation(fields: [runId], references: [id])

  media           Media[]
  transcripts     Transcript[]
  feedbacks       CommentFeedback[]
  inspoItems      InspoItem[]

  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  @@index([username])
}

model IgProfile {
  username        String   @id
  profileImageUrl String?
  lastScrapedAt   DateTime @default(now())

  posts           Post[]
  brandTargets    BrandTarget[]
}

model ScrapingRun {
  id          String   @id @default(uuid())
  username    String
  actorRunId  String?  @unique
  datasetId   String?
  rawData     Json?
  status      String   @default("completed")
  phase       String   @default("finished")
  isPaused    Boolean  @default(false)

  posts       Post[]

  createdAt   DateTime @default(now())
}

model Media {
  id          String   @id @default(uuid())
  postId      String
  post        Post     @relation(fields: [postId], references: [id])
  type        String
  url         String?
  storageUrl    String
  thumbnailUrl  String?
  duration      Float?
  index       Int      @default(0)

  transcript  Transcript?

  createdAt   DateTime @default(now())
}

model Transcript {
  id          String   @id @default(uuid())
  postId      String
  post        Post     @relation(fields: [postId], references: [id])

  mediaId     String?  @unique
  media       Media?   @relation(fields: [mediaId], references: [id])

  text        String
  originalText String?
  json        Json?

  createdAt    DateTime @default(now())
  embedding    Embedding?
}

model Embedding {
  id           String     @id @default(uuid())
  transcriptId String     @unique
  transcript   Transcript @relation(fields: [transcriptId], references: [id])
  vector       Unsupported("vector(1536)")
  model        String     @default("text-embedding-3-small")
  createdAt    DateTime   @default(now())
}

/// BrandIntelligence — nauthenticity's domain-specific intelligence layer for a brand.
/// The core Brand entity (name, isActive) is now owned by 9naŭ.
/// nauthenticity references brands exclusively via `brandId` (= 9naŭ Brand.id).
model BrandIntelligence {
  /// Primary key = 9naŭ Brand.id — no local structural duplication.
  brandId          String   @id

  /// workspaceId is kept as an operational routing reference for dispatch (not a structural copy).
  workspaceId      String   @default("")

  voicePrompt      String   @db.Text  // Brand DNA — primary personality prompt
  commentStrategy  String?  @db.Text
  suggestionsCount Int      @default(3)

  windowStart      String?
  windowEnd        String?
  /// timezone is an operational delivery-window field, owned by nauthenticity.
  timezone         String   @default("UTC")

  inspoRequestCount Int     @default(0)

  targets          BrandTarget[]
  feedbacks        CommentFeedback[]
  inspoItems       InspoItem[]
  syntheses        BrandSynthesis[]

  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt
}

model BrandSynthesis {
  id           String  @id @default(uuid())
  brandId      String
  brand        BrandIntelligence @relation(fields: [brandId], references: [brandId], onDelete: Cascade)
  type         String
  content      String  @db.Text
  attachedUrls Json    @default("[]")
  createdAt    DateTime @default(now())

  @@index([brandId, type])
}

model BrandTarget {
  id               String    @id @default(uuid())
  brandId          String
  brand            BrandIntelligence @relation(fields: [brandId], references: [brandId])
  username         String
  igProfile        IgProfile @relation(fields: [username], references: [username])
  profileStrategy  String?   @db.Text

  createdAt        DateTime @default(now())

  @@unique([brandId, username])
}

model CommentFeedback {
  id           String  @id @default(uuid())
  brandId      String
  brand        BrandIntelligence @relation(fields: [brandId], references: [brandId])
  postId       String
  post         Post    @relation(fields: [postId], references: [id])
  commentText  String  @db.Text
  isSelected   Boolean @default(false)
  sentAt       DateTime @default(now())
}

model InspoItem {
  id              String  @id @default(uuid())
  brandId         String
  brand           BrandIntelligence @relation(fields: [brandId], references: [brandId], onDelete: Cascade)
  postId          String?
  post            Post?   @relation(fields: [postId], references: [id])
  type            String
  note            String? @db.Text
  status          String  @default("pending")
  extractedHook   String? @db.Text
  extractedTheme  String?
  adaptedScript   String? @db.Text
  injectedContext String? @db.Text
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
}
